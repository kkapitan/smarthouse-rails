<h1>Users</h1>


<%= link_to "Create new user", "/users/new", class: "btn btn-lg btn-primary" %>
<ul class="users">
  <% @users.each do |user| %>
      <li>
        <%= "#{user.email}" %>
      </li>
  <% end %>
</ul>