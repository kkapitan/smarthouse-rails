<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for([:admin, @action_subject]) do |f| %>

        <%= f.label :name %>
        <%= f.text_field :name, class: 'form-control' %>

        <%= f.submit "Create subject", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>